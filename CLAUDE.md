# pointer_geocoding 開発ルール

## プロジェクト概要
QGISプラグイン「点群座標取得」(plugin_id: `pointer_geocoding`)。
アナログ図面上の連続打刻点から、基準点によるアフィン/ヘルマート座標変換を行い、CSV出力する。
言語: Python / 対象: QGIS 3.22〜3.99。

## ディレクトリ規約
- 対象コード: `src/`
- 設計書・参照ドキュメント: `docs/`（新設。既存の `dcs/integrated_master_design.md` も統合設計書として参照可能）

## 統括（このセッション）の役割
統括は以下のみを行う。**src配下のコードを直接編集しない**（Write/Editツールは使用禁止 = `disallowedTools: Write, Edit`）。

1. ユーザーとの対話・要件のヒアリング
2. 設計・実装方針の提案
3. `implementer` サブエージェントへの実装委譲（スコープを明確に切って依頼する）
4. `verifier` サブエージェントの静的検証結果の確認
5. 実行環境依存の確認項目がある場合、ユーザーへの人手確認依頼
6. 最終報告

実装が必要な作業は必ず `implementer` に委譲すること。統括自身がコードを書いてはならない。

## verifierの静的検証について（重要）
`verifier` が行うのは以下に限定した**静的検証**のみである。

- スコープ外のファイル変更がないか
- 設計書（`docs/`, `dcs/`）との論理的整合性
- 構文/型エラー、lintの通過
- 自動テストの結果（現状、自動テストコマンドは未設定）

**verifierの「静的検証: 問題なし」は、QGIS上での実際の動作を保証するものではない。**
UI操作・プラグインの実動作・図面上でのクリック/打刻挙動などは、静的検証の対象外であり、
別途「人手確認」ステップで人間がQGIS上で確認する。

## タスクの状態遷移
`.claude/state/tasks.md` で管理する。

```
承認待ち → 実装中 → 静的検証中 → 人手確認待ち → 完了
```

静的検証が「問題なし」でも、実行環境依存の確認項目が残っている限り自動的に「完了」にはしない。
人手確認が完了した旨をユーザーから確認できて初めて「完了」とする。

## 使い方
- `/explore-report <調査したい内容>` : explorerサブエージェントに調査を依頼
- `/implement-scope <実装スコープ>` : implementerサブエージェントに実装を依頼

## Git運用（ハイブリッド開発環境・test/main分離方式）
本プロジェクトは https://github.com/rei-watanabe-chiba/pointer_geocoding をバックアップ・複数端末同期の基盤とする。
複数セッション（クラウド/ローカル）が並行して動くことがあるため、ブランチの役割を以下の通り固定する。

### ブランチの役割
- **`test`（固定ブランチ）**: 動作確認前の改修コードを置くステージングブランチ。
  タスク実装中〜人手確認待ちの間は、常にこのブランチにコードをpushする。
- **`main`（固定ブランチ）**: 人手確認が完了し「動作確認済み」となったコードのみを置く安定版ブランチ。
  `test`から`main`への反映は、人手確認完了後にのみ行う（動作確認前のコードを直接`main`にpushしない）。
- 各セッションは、ハーネスが割り当てる作業ブランチ（例: `claude/xxxxx`）上でコミットしてよいが、
  **タスク完了時（pushする度）に必ず`test`ブランチへも反映する**
  （`git push origin <作業ブランチ>:test` でfast-forward、またはマージ後にpush）。
  これにより、`test`ブランチ名を知っていれば、どのセッションの作業ブランチ名を知らなくても
  ローカルで最新の未検証コードを取得できる。

### 開発フロー
1. コード改修は`implementer`が行い、完了時に作業ブランチへコミット、さらに`test`へも反映してpushする
   （git操作は自動実行し、pushの都度ユーザーに承認を求めない）。
2. ローカルでの動作確認前は、リポジトリ直下の **`sync_test.bat`** を実行して`test`ブランチを取得する。
3. ユーザーがQGIS上で人手確認を完了したら、統括（または指示を受けたimplementer）が
   `test`を`main`にfast-forwardマージしてpushする（`.claude/state/tasks.md`が「完了」になるタイミングと一致させる）。
4. `main`の内容だけを使ってローカル動作確認したい場合は **`sync_main.bat`** を実行する。

### 注意事項
- バッチファイルは文字化け・コマンド誤動作を避けるため、コメント等も含め**ASCII文字のみ**で記述し、
  改行コードはCRLFとする（過去に日本語コメント入りUTF-8ファイルがWindowsのANSIコードページ環境で
  文字化け・コマンド解釈エラーを起こした実績があるため）。
- `sync_test.bat`/`sync_main.bat`はいずれも、ローカルに未コミットの変更がある場合は同期を中断し警告する。
  また対象ブランチを`git reset --hard origin/<branch>`で強制的にリモートへ一致させるため、
  `test`/`main`ブランチ上でローカル独自にコミットを作らないこと（このリポジトリでの開発は必ず
  作業ブランチ上で行い、`test`/`main`は同期先としてのみ使う）。
