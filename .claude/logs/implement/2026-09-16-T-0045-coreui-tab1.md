## タスクID
T-0045（CoreUI試作、tab1_image.pyへ適用）

## 変更ファイル一覧
- 新規: `src/ui/core/__init__.py`
- 新規: `src/ui/core/field_spec.py`（`WidgetType` Enum, `ButtonDef`/`InfoLine`/`FieldSpec`/`PanelSpec` dataclass）
- 新規: `src/ui/core/builder.py`（`CoreUIBuilder.build(spec, parent) -> BuiltPanel`）
- 新規: `src/ui/core/rules.py`（`Rule` 抽象基底クラスのみ。tab1では具象ルールは未使用）
- 新規: `src/ui/tab1_image_schema.py`（Tab1の4パネル宣言: `TAB1_INFO_PANEL_SPEC` / `TAB1_MODE_TOGGLE_SPEC` /
  `TAB1_IMAGE_SECTION_SPEC` / `TAB1_TRANSFORM_SECTION_SPEC`）
- 変更: `src/ui/tab1_image.py`（`_create_tab1_ui()` を `CoreUIBuilder.build()` 経由の宣言的構築＋`bind()`
  によるイベント接続に置き換え。未使用となったPyQtウィジェットimport・`MAIN_RATIO`/`UIPlaceholders`
  importを削除。イベントハンドラ本体（`_on_tab1_mode_changed`以降のメソッド群）は無変更）

## 変更概要
`.claude/state/v2-coreui-plan.md` のアーキテクチャに沿って、`src/ui/core/` に宣言的UI構築エンジン
「CoreUI」を新設した。

- `FieldSpec`/`PanelSpec` は種別（`WidgetType`: LINEEDIT_ROW/COMBOBOX_ROW/BUTTON/BUTTON_ROW/TABLE/
  SEGMENTED_TOGGLE/INFO_PANEL）・ラベル・初期表示状態・イベントフック名のみを保持するデータクラスで、
  PyQtウィジェットを一切構築しない（構築はCoreUIBuilderの責務）。
- `CoreUIBuilder.build()` は内部で既存 `UIStyleHelper.build_flex_row` / `build_segmented_toggle` を
  そのまま部品として呼び出し、`ui/style.py` を置き換えるのではなく1段上に被せる設計とした。
- イベント接続は遅延方式: フィールド宣言時に登録した「pending connector」を、呼び出し側が
  `BuiltPanel.bind(hook_name, callback)` で実コールバックに接続する。これにより
  `tab1_image_schema.py` は `Tab1GeorefMixin` のメソッドを一切参照しない純粋な宣言ファイルとなった。
- `rules.py` はタスク指示通り、tab1で実際に使う具象ルールがないため `Rule` 抽象基底クラス（型の契約）
  のみを定義し、`ModeVisibilityRule`等の具象実装はT-0047へ委ねた（過剰実装回避）。Tab1の新規/編集
  モード切替（`_on_tab1_mode_changed`）はデータクリア等の画面固有副作用が多いため、CoreUIの外の
  素のPyQtコードとして`tab1_image.py`に残した（plan記載の「逃げ道」方針通り）。
- `tab1_image.py`側の`_create_tab1_ui()`は、4つのPanelSpecをbuildし、`get()`/`get_row()`/
  `get_buttons()`で既存のインスタンス属性名（`self.edit_image_path`, `self.row_edit_layer`,
  `self.table_ref_points`等）へ代入し直すのみとした。属性名・クラス名・イベントハンドラの中身は
  一切変更していない。
- 未使用になったlabel変数（`lbl_image_path`/`lbl_edit_layer`/`lbl_image_name`）は、他ファイルからの
  参照がないことを`grep`で確認した上で属性化を廃止した。

### 行数削減の実測値
- `src/ui/tab1_image.py`: 1151行 → 1058行（93行減、約8.1%減）
- 新設 `src/ui/tab1_image_schema.py`: 144行（宣言のみ）
- 新設 `src/ui/core/`: `field_spec.py` 159行、`builder.py` 284行、`rules.py` 37行、`__init__.py` 30行
  （エンジン自体はtab1_image.py以上のコード量になっているが、plan記載の通り「将来v2で新しいパネル・
  画面を追加するコストが下がる」ことが狙いであり、T-0046/T-0047で同エンジンを再利用することで
  エンジン新設コストを償却する設計）
- `_create_tab1_ui()`メソッド単体は約171行→約80行に削減された。

## 自動テスト実行結果
自動テストなし（プロジェクトに自動テストコマンドは設定されていない）。
`python3 -m py_compile` と `ast.parse()` による構文検証、および `python3 -m pyflakes` による
未使用import検出は実施し、いずれもエラーなし。

## スコープ外変更の有無
なし。変更ファイルは依頼スコープ通り `src/ui/core/`（新設）、`src/ui/tab1_image_schema.py`（新設）、
`src/ui/tab1_image.py`（改修）のみ（`git status --porcelain`で確認済み）。tab2_plot.py・
start_dialog.py・layer/系等には一切触れていない。

---

## 追記: T-0045 追加スコープ（業務ロジック共通化の第一段階）

### タスクID
T-0045（追加スコープ: 値の抜き出し/書き込み共通化）

### 変更ファイル一覧
- 変更: `src/ui/core/builder.py`
  - `BuiltPanel.__init__` に `field_types: Optional[Dict[str, WidgetType]]` 引数を追加し、
    `field_id -> WidgetType` の対応を保持するようにした。
  - `BuiltPanel.get_value(field_id) -> Any` を追加（LINEEDIT_ROW→`.text()`、COMBOBOX_ROW→
    `.currentText()`、SEGMENTED_TOGGLE→選択中ボタンのインデックス。他の種別は`NotImplementedError`）。
  - `BuiltPanel.set_value(field_id, value) -> None` を追加（`get_value`と対称。COMBOBOX_ROWは
    `findText`で該当インデックスが無ければ何もしない）。
  - `BuiltPanel.collect_values() -> Dict[str, Any]` を追加（`_VALUE_WIDGET_TYPES`＝
    LINEEDIT_ROW/COMBOBOX_ROW/SEGMENTED_TOGGLEに該当する登録済みフィールドを全て`get_value`）。
  - `CoreUIBuilder.build()`内のフィールドループで `field_types[f.field_id] = f.widget_type` を記録し、
    `BuiltPanel`構築時に渡すよう変更。
  - モジュールdocstringの`tab1_image_schema.py`参照を`schemas.py`に更新し、`get_value`等の追加を追記。
- 新規: `src/ui/schemas.py`（`tab1_image_schema.py`の内容を`# --- TAB1 ---`セクションとして移動。
  将来`# --- TAB2 ---`/`# --- TAB3 ---`/`# --- START_DIALOG ---`をT-0046/T-0047で追記する前提の構造）
- 削除: `src/ui/tab1_image_schema.py`
- 変更: `src/ui/tab1_image.py`
  - `from .tab1_image_schema import (...)` を `from .schemas import (...)` に変更。
  - `_create_tab1_ui()`内で`image_panel`（`TAB1_IMAGE_SECTION_SPEC`のBuiltPanel）を
    `self._tab1_image_panel`としてインスタンス属性に保持するよう変更。
  - 以下のhandlerで、キャッシュ済みウィジェットへの直接`.text()`/`.currentText()`呼び出しを
    `self._tab1_image_panel.get_value(field_id)`経由に置き換え（取得後の`.strip()`等の後処理は
    従来通りhandler側に残置。バリデーション・業務ロジック本体は無変更）。
    - `_on_edit_layer_changed`: `combo_edit_layer.currentText()` → `get_value("edit_layer")`
    - `_on_delete_layer_clicked`: 同上
    - `_on_rename_layer_clicked`: `combo_edit_layer.currentText()` → `get_value("edit_layer")`、
      `edit_image_name.text().strip()` → `get_value("image_name").strip()`
    - `_on_confirm_image_clicked`: `edit_image_name.text().strip()` → `get_value("image_name").strip()`、
      `edit_image_path.text().strip()` → `get_value("image_path").strip()`
    - `_on_export_layer_clicked`: `edit_image_name.text().strip()` → `get_value("image_name").strip()`
  - `self.combo_edit_layer`/`self.edit_image_name`/`self.edit_image_path`属性自体は引き続き
    `image_panel.get(...)`で代入されたまま残しており（`_on_tab1_mode_changed`等の`.clear()`呼び出し
    や、テーブル再描画コードが今回のスコープ外のため）、削除していない。

### 変更概要
`.claude/state/v2-coreui-plan.md`「T-0045 追加スコープ」節に従い、`BuiltPanel`に値系フィールド
（LINEEDIT_ROW/COMBOBOX_ROW/SEGMENTED_TOGGLE）の汎用アクセサを追加し、tab1側の該当handlerを
`panel.get_value()`経由に置き換えた。TABLE型は複雑な行構造のため対象外のまま。storage/upload側の
汎用ルール化（CommitRule等）はplan記載の通りT-0047まで据え置き、今回は一切手を付けていない。

### 行数の変化
- `src/ui/core/builder.py`: 284行 → 360行（+76行、`get_value`/`set_value`/`collect_values`と
  docstring追加分）
- `src/ui/tab1_image.py`: 1058行 → 1059行（ほぼ横ばい。`self._tab1_image_panel = image_panel`の
  1行追加に対し、`.currentText()`/`.text()`直接呼び出しを`get_value(...)`に置換した行は行数不変）
- 新設`src/ui/schemas.py`: 153行（旧`tab1_image_schema.py`144行から、コメント追加・
  `tab1_image_schema.py`参照の`schemas.py`への置換分で微増）
- 旧`src/ui/tab1_image_schema.py`（144行）は削除

### 自動テスト実行結果
自動テストなし（プロジェクトに自動テストコマンドは設定されていない）。
`python3 -c "ast.parse(...)"`による構文検証と`python3 -m pyflakes src/ui/tab1_image.py
src/ui/core/builder.py src/ui/schemas.py`による未使用import/変数検出を実施し、いずれもエラー・
警告なし。

### スコープ外変更の有無
なし。変更対象は依頼スコープ通り`src/ui/core/builder.py`・`src/ui/tab1_image.py`・
`src/ui/schemas.py`（新設）・`src/ui/tab1_image_schema.py`（削除）のみ。`src/ui/core/field_spec.py`・
`src/ui/core/rules.py`のdocstring中に残る`tab1_image_schema.py`という文字列（コメントのみ、
importパス等の実体参照ではない）は今回のスコープ外ファイルのため修正していない。
