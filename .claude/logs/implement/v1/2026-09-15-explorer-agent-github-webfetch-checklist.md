## 人手確認チェックリスト

### 確認手順
本タスクは `.claude/agents/explorer.md` というエージェント定義（設定ファイル）のテキストを置き換えたのみであり、
QGIS上で動作するプラグイン本体のPythonコード（`src/`）や設計書（`docs/`）には一切変更を加えていない。
そのため、QGIS上でのプラグイン実行環境に依存する確認項目は存在しない。

該当なし（本タスクはQGIS実行環境に一切影響しないエージェント設定ファイルの変更のため、QGIS上での動作確認手順は不要）。

強いて確認するとすれば、以下はCLAUDE.md運用上の確認であり、QGIS実行とは無関係である。
1. `.claude/agents/explorer.md` を開き、依頼された内容（GitHubモードの追加、`tools` への `WebFetch` 追加）が反映されていることを目視確認する。
2. 次回 `explorer` サブエージェントにGitHubのURLを指定して調査を依頼し、`WebFetch` を用いて調査モードが正しく切り替わり、出力形式（対象範囲/関連ファイル一覧/実装概要/論点）に沿って報告されることを確認する（任意・運用上の動作確認であり、QGIS実行環境とは無関係）。

### 各手順で期待される挙動（設計書ベース）
- 本変更はCLAUDE.mdが定める「統括の役割」（explorerサブエージェントへの調査委譲の仕組み）に関するエージェント設定ファイルの更新であり、QGISプラグイン本体（`src/`）や統合設計書（`dcs/integrated_master_design.md`）が定める点群座標取得プラグインの機能・挙動には一切関与しない。そのため、設計書に基づくQGIS上の期待挙動の記載事項は存在しない。

### 注意喚起
- 本変更は `.claude/agents/explorer.md` のみに閉じており、`src/` 配下のプラグイン本体コードには一切影響しない。md5sum比較により、`.claude/` 配下の他ファイル（`CLAUDE.md` は元々対象外、`implementer.md`、`verifier.md`、`settings.json`、`tasks.md`、既存ログ類）に差分がないことを確認済みである。
- `explorer` エージェントの `tools` に `WebFetch` が追加されたことで、今後explorerがGitHub上の公開リポジトリへ外部通信を行う可能性がある点は運用上の注意点である。ただし本タスクの依頼内容（読み取り専用・非公開リポジトリへのアクセス試行禁止を明記）の範囲内であり、`disallowedTools: Write, Edit` は変更していないため、書き込み系操作は引き続き禁止されている。
