## 人手確認チェックリスト

### 確認手順
1. `.claude/state/tasks.md` を開き、T-0007の行が表に追加されていることを目視で確認する。
2. T-0007の行の各列（内容／状態／実装ログ／検証結果／人手確認）が、依頼内容
   （Stage F: `_on_canvas_clicked()`の責務分解、`get_next_point_id()`・
   `insert_feature_to_layer()`の`core_logic.py`への抽出、状態「完了」）と一致していることを確認する。
3. `.claude/state/tasks.md` 以外のファイル（`src/`配下含む）に差分が発生していないことを確認する
   （本作業はコード変更を伴わないため）。

### 各手順で期待される挙動（設計書ベース）
- 手順1・2: `CLAUDE.md`「タスクの状態遷移」節に基づき、`.claude/state/tasks.md` は
  「承認待ち → 実装中 → 静的検証中 → 人手確認待ち → 完了」の状態遷移を記録する表であり、
  T-0007はユーザーからの申告（人手確認完了「動作確認した。問題ない」）に基づき
  「完了」状態として記載されているべきである。
- 手順3: 本依頼は「タスク状態ファイルの更新のみ」「src配下のコードは一切変更しない」との
  明示的なスコープ指定であり、`.claude/state/tasks.md` 以外への変更が存在しないことが
  スコープ遵守の確認事項となる。

### 注意喚起
- 本作業はQGIS上での実際の動作に影響を与えるコード変更を一切含まないため、
  QGIS上での実機確認は本質的に不要と考えられる。上記手順は状態ファイルの記載内容の
  正確性を目視確認するものである。
- T-0007自体（`get_next_point_id()`／`insert_feature_to_layer()`への責務分解）の実装内容・
  人手確認結果は、本ログの対象外であり、`.claude/logs/implement/2026-09-15-stageF-on-canvas-clicked-decomposition.md`
  側の記載を参照すること。
